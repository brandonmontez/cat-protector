## What does this change?

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `make test` passes
- [ ] `make app` builds and the app still locks and unlocks with the shortcut
- [ ] Lock logic changes come with a test in `Tests/CatProtectorTests`
- [ ] No networking, file writing, or process spawning was added to `Sources/` (the trust test will catch it)
- [ ] README updated if behaviour changed
